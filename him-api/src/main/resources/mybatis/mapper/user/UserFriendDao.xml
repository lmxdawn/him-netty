<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.lmxdawn.him.api.dao.user.UserFriendDao">

    <select id="listByUid" parameterType="com.lmxdawn.him.api.req.UserFriendListRequest" resultType="com.lmxdawn.him.common.entity.user.UserFriend">
        select id,uid,`friend_uid`,remark
        from `user_friend`
        where uid = #{uid}
        limit #{offset},#{limit}
    </select>

    <select id="findByUidAndFriendUid" resultType="com.lmxdawn.him.common.entity.user.UserFriend">
        select id
        from `user_friend`
        where uid = #{uid} and friend_uid=#{friendUid}
        limit 1
    </select>

    <insert id="insertUserFriend" keyProperty="id" parameterType="com.lmxdawn.him.common.entity.user.UserFriend">
        insert into `user_friend`(`uid`,friend_uid,remark,create_time,modified_time)
        values
        (#{uid},
        #{friendUid},
        #{remark},
        #{createTime},
        #{modifiedTime})
    </insert>

    <update id="updateUserFriend" parameterType="com.lmxdawn.him.common.entity.user.UserFriend">
        update `user_friend`
        <set>
            <if test="remark != null">`remark`=#{remark},</if>
            modified_time=#{modifiedTime}
        </set>
        where uid = #{uid} and friend_uid = #{friendUid}
    </update>

    <delete id="deleteByUidAndFriendUid">
        delete from `user_friend` where uid = #{uid} and friend_uid = #{friendUid}
    </delete>

</mapper>